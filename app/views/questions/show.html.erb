<h1>Question show page</h1>
<p>/categories/:category_id/questions/:id</p>

<h2><%= @question.title%></h2>
<p><%= @question.content%></p>

<button type="button" class="btn btn-secondary btn-sm"><%= link_to 'Edit question' , edit_category_question_path(@question.category, @question) %></button>
<%= button_to "Detele", category_question_path(@question.category, @question),:method => :delete, :class => "btn btn-secondary btn-sm"%>
<%#= form_tag(category_question_path(@question.category, @question), :confirm => "Are you sure?", :method => :delete) do %>
    <!--<button type="submit" class=" btn btn-secondary btn-sm">Delete</button>!-->
<%# end %>